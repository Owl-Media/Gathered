/**
 * Test stub for the `server-only` package.
 *
 * The real module throws on import outside a React Server Component graph. That
 * guard protects the production build; in unit tests it would simply prevent
 * server modules from being exercised at all.
 */
export {};
